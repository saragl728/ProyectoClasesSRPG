package com.example.proyectoclasessrpg.provider

import com.example.proyectoclasessrpg.database.ClasePropiedad

class ClasePropiedadProvider {
    companion object {
        var listaClasePropiedad = listOf(ClasePropiedad())
    }
}